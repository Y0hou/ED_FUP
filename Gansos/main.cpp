#include<bits/stdc++.h>
#include <iostream>
#include <sstream>

using namespace std;

int main() {
    string str, aux1, compara;
    stringstream ss;

    getline(cin, str);

    ss << str;
    ss >> aux1;

    while(aux1 >> compara){

    }
    return 0;
}